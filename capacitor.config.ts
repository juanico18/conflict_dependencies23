import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'test_conflicts',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
